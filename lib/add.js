/**
 * Add a new user profile object to couchdb
 */
var rk = require('required-keys');
var should = require('should');
var inspect = require('eyespect');
module.exports = function (data, cb) {
  var keys = ['db', 'email'];
};
